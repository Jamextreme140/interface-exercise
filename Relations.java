package exercise;

public interface Relations {
	//Returns true if A is more than B
	boolean isMore(Object b);
	//Returns true if A is less than B
	boolean isLess(Object b);
	//Returns true if A equals B
	boolean equals(Object b);

}
